# Attention LIVE 环境变量配置

# 使用 Mock 数据 (开发模式)
NEXT_PUBLIC_USE_MOCK=true

# API 后端地址
NEXT_PUBLIC_API_URL=http://localhost:8000/api

# 币安 OAuth (预留)
NEXT_PUBLIC_BINANCE_CLIENT_ID=your_client_id
NEXT_PUBLIC_BINANCE_REDIRECT_URI=http://localhost:3000/auth/callback

# 合约地址 (预留)
NEXT_PUBLIC_CONTRACT_ADDRESS=0x...
NEXT_PUBLIC_CHAIN_ID=56
